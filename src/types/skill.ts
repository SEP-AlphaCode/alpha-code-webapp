export type Skil = {
    id: string
    code: string
    name: string
    description: string
    status: number
    icon: string
    createdDate: string
    lastUpdate: string
    duration: number
}

export type SkillModal = {
    code: string
    name: string
    description: string
    duration: number
    status: number
    icon: string
}