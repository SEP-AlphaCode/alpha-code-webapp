export type Dance = {
    id: string
    code: string
    name: string
    description: string
    status: number
    icon: string
    createdDate: string
    lastUpdate: string
    duration: number
}

export type DanceModal = {
    code: string
    name: string
    description: string
    duration: number
    status: number
    icon: string
}