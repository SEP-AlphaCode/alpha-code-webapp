export type Dance = {
    id: string
    name: string
    description: string
    status: number
    createdDate: string
    lastUpdate: string
    duration: number
}

export type DanceModal = {
    name: string
    description: string
    duration: number
    status: number
}