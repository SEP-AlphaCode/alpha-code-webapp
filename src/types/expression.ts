export type Expression = {
    id: string
    name: string
    imageUrl: string
    description: string
    status: number
    createdDate: string
    lastUpdate: string
}

export type ExpressionModal = {
    name: string
    imageUrl: string
    description: string
    status: number
}